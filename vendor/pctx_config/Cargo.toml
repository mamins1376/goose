# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2024"
name = "pctx_config"
version = "0.1.5"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "Configuration management for pctx toolkit"
readme = false
keywords = [
    "pctx",
    "config",
    "mcp",
]
categories = ["development-tools"]
license = "MIT"
repository = "https://github.com/portofcontext/pctx"
resolver = "2"

[lib]
name = "pctx_config"
path = "src/lib.rs"

[dependencies.anyhow]
version = "1"

[dependencies.base64]
version = "0.22"

[dependencies.camino]
version = "1"
features = ["serde1"]

[dependencies.http]
version = "1"

[dependencies.indexmap]
version = "2"
features = ["serde"]

[dependencies.keyring]
version = "3"
features = [
    "apple-native",
    "windows-native",
    "linux-native",
]

[dependencies.opentelemetry-otlp]
version = "0.31"
features = [
    "grpc-tonic",
    "http-proto",
    "trace",
]

[dependencies.opentelemetry_sdk]
version = "0.31"

[dependencies.reqwest]
version = "0.13.2"
features = [
    # Upstream pctx_config asks for reqwest/rustls. In this fork that is the
    # ONLY thing that drags rustls -> hyper-rustls -> aws-lc-rs into the build,
    # which is a whole second TLS stack (and a large C crypto library) next to
    # the OpenSSL one the `native-tls` feature selects. reqwest is a single
    # crate in the graph, so swapping this feature makes every HTTP client in
    # the build use OpenSSL. pctx_config's own source never names rustls, so
    # this is a feature-list change only — no code is altered.
    "native-tls",
    "json",
    "cookies",
    "gzip",
    "brotli",
    "deflate",
    "zstd",
    "charset",
    "http2",
    "stream",
    "blocking",
]
default-features = false

[dependencies.rmcp]
version = "1.2.0"
features = [
    "client",
    "transport-streamable-http-client-reqwest",
    "transport-child-process",
]

[dependencies.serde]
version = "1"
features = ["derive"]

[dependencies.serde_json]
version = "1"

[dependencies.shlex]
version = "1"

[dependencies.thiserror]
version = "2"

[dependencies.tokio]
version = "1"
features = [
    "macros",
    "rt",
    "process",
]

[dependencies.tonic]
version = "0.14"

[dependencies.tracing]
version = "0.1"

[dependencies.url]
version = "2"
features = ["serde"]

[build-dependencies]

[lints.clippy]
cast_possible_truncation = "allow"
cast_possible_wrap = "allow"
cast_precision_loss = "allow"
cast_sign_loss = "allow"
match_same_arms = "allow"
module_name_repetitions = "allow"
must_use_candidate = "allow"
similar_names = "allow"
too_many_lines = "allow"

[lints.clippy.all]
level = "warn"
priority = -1

[lints.clippy.pedantic]
level = "warn"
priority = -1

[lints.rust]
unreachable_pub = "warn"
